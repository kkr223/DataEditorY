# src/lib/modules/settings/workbench/

## Responsibility
Settings Workbench — the settings panel interface.

## Components
- `SettingsWorkbench.svelte` — renders settings header and all registered settings sections via platform
- `context.ts` — settings workbench context provider
