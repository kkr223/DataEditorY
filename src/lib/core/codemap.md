# src/lib/core/

## Responsibility

Core Abstractions Layer — defines workspace projections, CDB path identity, and editor snapshots used by the CDB-first shell. The projection can still represent compatibility document types, but top-level shell tabs are CDB workspaces.

## Integration

- **Sub-map**: [workspace/](workspace/codemap.md)
