# src/lib/actions/

## Responsibility

Svelte Actions — reusable DOM behavior actions applied via `use:action` directive.

## Files

- `disableAutofill.ts` — disables browser autofill on form fields by setting appropriate attributes

## Integration

- **Consumed by**: form components (card editor, settings)
